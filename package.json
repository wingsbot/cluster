{
  "name": "wings",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm turbo run build",
    "start-bot": "tsc && nodemon --watch 'dist/**/*' dist/index.js",
    "start-webhook-server": "tsc && nodemon --watch 'dist/**/*' dist/index.js",
    "update-prisma": "prisma format --schema=./packages/database/schema.prisma && prisma generate --schema=./packages/database/schema.prisma && prisma db push --schema=./packages/database/schema.prisma",
    "lint": "eslint . --ext .ts --fix",
    "build-bot": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.25.0",
    "@typescript-eslint/parser": "^5.25.0",
    "eslint": "^8.15.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-unicorn": "^42.0.0",
    "turbo": "^1.2.9",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@prisma/client": "^3.13.0",
    "prisma": "^3.13.0"
  }
}
