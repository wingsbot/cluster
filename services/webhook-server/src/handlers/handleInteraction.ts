import type { FastifyReply, FastifyRequest } from "fastify";

export default function(request: FastifyRequest, reply: FastifyReply) {
  // const signature = request.headers['X-Signature-Ed25519'] as string; and stuff
}